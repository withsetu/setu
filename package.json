{
  "name": "saytu",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "description": "Open-source, Git-backed, multi-topology CMS engine.",
  "license": "AGPL-3.0-only",
  "scripts": {
    "dev": "concurrently -n api,admin,site -c blue,magenta,green \"SAYTU_API_PORT=4444 SAYTU_REPO_DIR=$PWD pnpm --filter @setu/api dev\" \"VITE_SAYTU_API=http://localhost:4444 pnpm --filter @setu/admin dev\" \"pnpm --filter @setu/site dev\"",
    "test": "pnpm -r test",
    "typecheck": "pnpm -r typecheck"
  },
  "devDependencies": {
    "concurrently": "^9.1.0",
    "typescript": "^5.6.3"
  },
  "packageManager": "pnpm@10.33.0",
  "pnpm": {
    "onlyBuiltDependencies": ["esbuild", "better-sqlite3", "sharp"]
  }
}
