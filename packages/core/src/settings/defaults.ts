import type { SiteSettings } from './types'

export const DEFAULT_SETTINGS: SiteSettings = {
  general: {
    title: 'Setu',
    tagline: '',
    description: '',
    timezone: 'UTC',
    dateFormat: 'MMM D, YYYY',
  },
}
